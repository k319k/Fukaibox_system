あなたには封解Boxを作ってもらう。

まず、一番最初にアクセスしたら儀長モードか一般モードかを選ぶ→if儀長then Discordログインで儀長かを確認→儀長なら儀長モードで入れる

一番最初にアクセスしたら一般モード→If 儀長が儀員以外立ち入り禁止モードを選択していた then Discordログイン→DiscordBotが儀員ロール持ってるか確認→あったら入れる 儀員以外立ち入り禁止モードでなければユーザー名を入力→入れる(discordも可)

儀長が入った時、ホーム→ボタン押されたらボックスを作成して台本画面に移動→台本を書く（この時に文字数を表示、改行できるようにする）→画像素材up画面（以下と同様）

一般モードで入った時、ホーム→議長が作ったシートに入る→儀長が台本を書き終わったら→画像素材up画面

画像素材up画面→儀長が台本をもとに何個かの文章に分ける→その文章ごとに一般が画像をupできるようにする（議長が設定すればだれにも上げられないようにすることも可能）→儀長がどれを採用か決定する→おわり→シートアーカイブ

シートの採用画像をzipでまとめてダウンロード、640*480にしてzipでまとめてダウンロード（いらない部分切り捨て）、ひとつづつダウンロード、ひとつづつ640*480ダウンロードができるように

シートアーカイブ後は任意のユーザーのGoogle driveにバックアップとっといて

ログインは継続されるようにして

discordログインした人は画像をupするごとに5点、採用されたら追加で20点（ここらへんんは儀長がwebで変えられるようにする）

儀長は任意のユーザーをブロックできるようにして

儀長は点数を自由に変えられるように

ログインした人は点数を確認できるように

discordログインができるように

あとブラウザでテストして
